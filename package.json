{
  "name": "postcss-preset-moxy",
  "description": "PostCSS preset to be used at MOXY",
  "version": "4.1.2",
  "keywords": [
    "postcss",
    "preset",
    "react",
    "moxy",
    "transpile"
  ],
  "author": "André Cruz <andre@moxy.studio>",
  "homepage": "https://github.com/moxystudio/postcss-preset-moxy",
  "repository": {
    "type": "git",
    "url": "git@github.com:moxystudio/postcss-preset-moxy.git"
  },
  "license": "MIT",
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "scripts": {
    "lint": "eslint .",
    "test": "jest --env node --coverage",
    "prerelease": "npm t && npm run lint",
    "release": "standard-version"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "commit-msg": "commitlint -e $GIT_PARAMS"
    }
  },
  "standard-version": {
    "scripts": {
      "posttag": "git push --follow-tags origin master"
    }
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "git add"
    ]
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "dependencies": {
    "browserslist-config-google": "^1.5.0",
    "postcss-color-function": "^4.0.1",
    "postcss-conditionals": "^2.1.0",
    "postcss-css-variables": "^0.13.0",
    "postcss-import": "^12.0.0",
    "postcss-mixins": "^6.2.0",
    "postcss-preset-env": "^6.6.0",
    "postcss-url": "^8.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.0.0",
    "@commitlint/config-conventional": "^8.0.0",
    "eslint": "^5.3.0",
    "eslint-config-moxy": "^7.0.0",
    "husky": "^2.2.0",
    "jest": "^24.5.0",
    "jest-serializer-path": "^0.1.15",
    "lint-staged": "^8.1.3",
    "lodash.compact": "^3.0.1",
    "postcss": "^7.0.5",
    "standard-version": "^6.0.1"
  }
}
